import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.marketplace.capacitor.initpay.user',
  appName: 'initPay',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
